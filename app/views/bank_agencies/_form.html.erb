<%= form_with(model: bank_agency, class: "w-75") do |form| %>
  <% if bank_agency.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bank_agency.errors.count, "error") %> prohibited this bank_agency from being saved:</h2>

      <ul>
        <% bank_agency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :agency_number, class: "form-label" %>
    <%= form.text_field :agency_number, class: "form-control"%>
  </div>

  <div class="mb-3">
    <%= form.label :address, class: "form-label" %>
    <%= form.text_field :address, class: "form-control"%>
  </div>

  <div>
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>



